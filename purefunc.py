# A pure function: depends only on its inputs
def add_numbers(a, b):
    return a + b

# Function calls
print(add_numbers(2, 3))   # Output: 5
print(add_numbers(5, 3))   # Output: 5 (same input → same output)
